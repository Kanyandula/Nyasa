package com.kanyandula.nyasa.ui.theme.window

/**
 * Coarse window-size buckets. Small = phones, Medium = tablets and phone-landscape with
 * limited vertical space. NyasaBlog targets phones + tablets only — TV/AAOS classes
 * intentionally omitted.
 */
enum class WindowSizeClass { Small, Medium }
